import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app') 
// เรียก id app เอา vue App มา mount กับ id app
// mount คือการเอามาต่อกัน
// ทุกอย่าที่เขียนจะไปส่งผลใน div id app ใน index.html
